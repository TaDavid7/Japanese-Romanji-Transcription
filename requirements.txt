# Python 3.12

# --- speech recognition ---
faster-whisper==1.2.1

# --- CUDA runtime libs (see cuda_setup.py; drop these for CPU-only) ---
nvidia-cublas-cu12
nvidia-cudnn-cu12==9.*

# --- Japanese text processing ---
fugashi==1.5.2        # MeCab bindings
unidic-lite==1.0.8    # MeCab dictionary (fugashi finds it automatically)
jaconv==0.5.0         # kana <-> kana <-> romaji conversion
pykakasi==2.3.0       # character-level romanizer (stages 1 & 3 baseline)
jamdict==0.1a11.post2 # JMdict homograph lookup (hybrid route)
# jamdict-data is NOT installable via pip reliably -- see README "JMdict database"

# --- audio i/o (live pipeline) ---
sounddevice
numpy

# --- evaluation & data ---
jiwer                 # character error rate
pandas
pyyaml                # reads jsut-label annotations
